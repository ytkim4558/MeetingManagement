<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nagnek.android.meetingmanagement">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GroupActivity" />
        <activity
            android:name=".NewGroupPopupActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".EditMemberActivity"
            android:label="@string/title_activity_new_member_name_popup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MemberItemPopupMenuActivity"
            android:label="@string/title_activity_member_item_popup_menu"
            android:theme="@android:style/Theme.Dialog"></activity>
    </application>

</manifest>